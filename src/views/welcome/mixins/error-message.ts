export default {
    computed: {
        // errorMessageStartDate() {
        //     const errors = [];
            
        //     if (!this.$v.entity.startDate.$dirty) return errors;

        //     !this.$v.entity.startDate.required && errors.push('Lütfen bir tarih seçiniz!');
        //     !this.$v.entity.startDate.minLength && errors.push('Tarih eksik!');
        //     !this.$v.entity.startDate.maxLength && errors.push('Tarih eksik!');

        //     return errors;
        // },

        // errorMessageEndDate() {
        //     const errors = [];
            
        //     if (!this.$v.entity.endDate.$dirty) return errors;

        //     !this.$v.entity.endDate.required && errors.push('Lütfen bir tarih seçiniz!');
        //     !this.$v.entity.endDate.minLength && errors.push('Tarih eksik!');
        //     !this.$v.entity.endDate.maxLength && errors.push('Tarih eksik!');

        //     return errors;
        // }
    }
}